function updateTable() {
    // Make an AJAX request to a PHP script to fetch the latest team data from the database
    // On successful retrieval, update the table body with the new data
   }
   
   // Call the function every few seconds to update the table
   setInterval(updateTable, 10000); // 10 seconds